/** @type {import('next').NextConfig} */
const nextConfig = {
  // Transpile packages from the monorepo
  transpilePackages: [],
  // Output standalone for Vercel optimization (optional but recommended)
  // output: 'standalone',
};

export default nextConfig;
