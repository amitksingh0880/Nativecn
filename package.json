{
  "name": "nativecn-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "overrides": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "@nativecn/docs": {
      "react": "19.0.0",
      "react-dom": "19.0.0"
    },
    "@nativecn/ui": {
      "react": "19.0.0",
      "react-dom": "19.0.0"
    }
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "registry:generate": "npm --prefix apps/docs run registry:generate",
    "registry:watch": "npm --prefix apps/docs run registry:generate -- --watch",
    "test": "turbo run test",
    "test:coverage": "turbo run test -- --coverage",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "rimraf": "^6.0.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4"
}
